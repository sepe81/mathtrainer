# AGENTS.md

## Workflow rules

- Every new requirement introduced during prompting must be added to `requirements.md` immediately.
- After every change to a Markdown file, run `npx markdownlint-cli "**/*.md"` and fix all reported errors.
